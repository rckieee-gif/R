# Optional. Leave blank to call the same origin; Vite proxies /api to localhost:5000 in development.
# Set this when the frontend is deployed separately from the API.
VITE_API_BASE=
# Example: VITE_API_BASE=https://octavio-farm-api.onrender.com
